name: Build TQUIC v1.6.0 iOS SDK

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build-ios:
    runs-on: macos-latest
    env:
      RUSTUP_HOME: ${{ github.workspace }}/.rustup
      CARGO_HOME: ${{ github.workspace }}/.cargo

    steps:
      # 1️⃣ Checkout v1.6.0 tag with submodules
      - name: Checkout TQUIC v1.6.0
        uses: actions/checkout@v4
        with:
          repository: Tencent/tquic
          ref: v1.6.0
          submodules: recursive

      # 2️⃣ Install Xcode CLI tools
      - name: Install Xcode Command Line Tools
        run: |
          xcode-select --install || echo "Xcode CLI already installed"

      # 3️⃣ Accept Xcode license
      - name: Accept Xcode license
        run: sudo xcodebuild -license accept

      # 4️⃣ Set SDKROOT
      - name: Set SDKROOT
        id: sdk
        run: |
          echo "SDKROOT=$(xcrun --sdk iphoneos --show-sdk-path)" >> $GITHUB_ENV
          echo "iOS SDKROOT set to $(xcrun --sdk iphoneos --show-sdk-path)"

      # 5️⃣ Install Rust stable
      - name: Install Rust
        uses: actions-rs/toolchain@v1
        with:
          toolchain: stable
          override: true

      # 6️⃣ Add iOS Rust targets
      - name: Add iOS targets
        run: |
          rustup target add aarch64-apple-ios x86_64-apple-ios aarch64-apple-ios-sim

      # 7️⃣ Install cargo-lipo
      - name: Install cargo-lipo
        run: cargo install cargo-lipo

      # 8️⃣ Build iOS universal library (verbose) ✅ Deployment target set here
      - name: Build TQUIC universal library
        run: |
          echo "Building TQUIC universal library with minimum iOS deployment target 10.0"
          set -o pipefail
          cargo lipo \
            --release \
            --features ffi \
            --verbose \
            --targets aarch64-apple-ios,x86_64-apple-ios,aarch64-apple-ios-sim \
            || (echo "BUILD FAILED"; tail -n 50 target/universal/release/build/tquic-*/output.log; exit 1)
        env:
          IPHONEOS_DEPLOYMENT_TARGET: 10.0
          SDKROOT: ${{ env.SDKROOT }}

      # 9️⃣ Prepare SDK structure
      - name: Prepare SDK layout
        run: |
          mkdir -p TQUIC-iOS/lib
          mkdir -p TQUIC-iOS/include
          cp target/universal/release/*.a TQUIC-iOS/lib/libtquic.a
          cp -R include/* TQUIC-iOS/include/

      # 🔟 Zip SDK
      - name: Zip SDK
        run: zip -r TQUIC-iOS-v1.6.0.zip TQUIC-iOS

      # 1️⃣1️⃣ Upload artifact
      - name: Upload SDK artifact
        uses: actions/upload-artifact@v4
        with:
          name: tquic-ios-sdk-v1.6.0
          path: TQUIC-iOS-v1.6.0.zip
